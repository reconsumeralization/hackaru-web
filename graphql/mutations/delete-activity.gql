mutation deleteActivity(
  $id: ID!
) {
  deleteActivity(input: {
    id: $id
  }) {
    activity {
      id
      description
      startedAt
      stoppedAt
      project {
        id
        name
        color
      }
    }
  }
}
